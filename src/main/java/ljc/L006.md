L006. 轮转数组 🔄

    将数组中的元素向右轮转 k 个位置。
核心思路

    三步反转法 (最优解)：

        反转整个数组。

        反转前 k 个元素。

        反转剩下的元素。

    额外数组法：使用 (i + k) % n 公式计算新位置。

复杂度分析

    时间复杂度: O(n) — 每个元素反转两次。

    空间复杂度: O(1) — 原地反转不需要额外空间。

关键点总结

    取模运算：k %= n 是为了处理 k 超过数组长度的情况。